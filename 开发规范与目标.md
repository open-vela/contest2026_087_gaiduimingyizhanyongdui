# 郭黄亦昕 开发规范

> 以下是你需要遵守的接口契约、数据约定和开发约束。
> 遵循这些规范，你的代码就能和其他四个人的代码无缝集成。

---

## 一、你能调用的接口（别人写好给你的）

这些接口在 `api/lcd.h` 和 `api/audio.h` 中声明，你来实现 `.c` 文件。

```c
// ============ LCD 显示接口 ============

// 初始化 LCD，只调用一次
// 返回: 0=成功, 负值=错误码
int  lcd_init(void);

// 更新监测主画面的全部内容（由状态机 1Hz 定时调用）
// st: 当前设备状态 (IDLE/MONITORING/REPORT/...)
// s:  当前会话统计数据
// obs: 最新一帧的 Observation (用于决定显示哪个图标)
// 返回: 0=成功
int  lcd_show_status(device_status_t st, session_stats_t *s,
                     study_state_t *ss);

// 显示学习报告（学习结束时调用一次）
// r:    报告数据
// advice: MiMo 返回的建议文本 (可为 NULL 表示未获取到)
// 返回: 0=成功
int  lcd_show_report(session_report_t *r, const char *advice);

// 显示全屏提示消息（提醒/鼓励时调用，持续 3 秒后自动返回主画面）
// msg: 要显示的文本 (来自赵思涵的 study_state_t.message)
// type: 消息类型 (REMIND=警告样式, ENCOURAGE=鼓励样式)
// 返回: 0=成功
int  lcd_show_message(const char *msg, action_t type);

// ============ 音频接口 (可选，与张沐泽协作) ============

// 播放提醒/鼓励语音
// msg: 文本内容 (TTS 合成或预录音频)
// type: REMIND 或 ENCOURAGE
int  audio_play_alert(const char *msg, action_t type);
```

---

## 二、你需要消费的数据结构（别人产出的）

```c
// ── 来自万思源 (state_machine.h) ──

typedef enum {
    DEVICE_IDLE,
    DEVICE_MODE_SELECT,
    DEVICE_MONITORING,
    DEVICE_REPORT
} device_status_t;

typedef struct {
    uint32_t session_start;          // 学习开始时间戳
    uint32_t total_duration_sec;     // 总时长 (秒)
    uint32_t effective_duration_sec; // 有效学习时长 (秒)
    uint8_t  distraction_count;      // 分心总次数
    uint8_t  focus_score;            // 专注度评分 0-100
    uint8_t  current_mode;           // 0=STRICT, 1=GENTLE
} session_stats_t;

typedef struct {
    session_stats_t stats;
    uint8_t  distraction_by_type[4]; // [玩手机, 看手机, 离座, 瞌睡]
    char     advice[512];            // MiMo 建议
} session_report_t;


// ── 来自赵思涵 (behavior.h) ──

typedef enum { FOCUSED, GLANCING_PHONE, PLAYING_PHONE, AWAY, DROWSY } status_t;
typedef enum { NONE, REMIND, ENCOURAGE } action_t;

typedef struct {
    status_t  status;              // 当前学习状态
    action_t  action;              // 应采取的行动
    char      message[128];        // 提醒/鼓励文字 (★ 直接显示在 LCD 上)
    bool      milestone_reached;   // 是否达到专注里程碑 (鼓励模式专属)
    int       milestone_minutes;   // 连续专注分钟数
    uint8_t   focus_score_delta;   // 评分变化量
} study_state_t;
```

---

## 三、你需要实现的页面

### 3.1 待机画面 (`DEVICE_IDLE`)

```
┌────────────────────┐
│                    │
│    FOCUS AIoT      │  ← 产品名, 24pt
│                    │
│   [设备简笔画]      │  ← 可选
│                    │
│ 长按开始键进入模式  │  ← 提示文字, 12pt
│                    │
└────────────────────┘
```

### 3.2 模式选择画面 (`DEVICE_MODE_SELECT`)

```
┌────────────────────┐
│    选择学习模式     │
│                    │
│  ┌──────┐ ┌──────┐ │
│  │ 严格  │ │ 鼓励  │ │  ← 选中项高亮 (绿色边框)
│  │ 模式  │ │ 模式  │ │
│  └──────┘ └──────┘ │
│  严格监督  温暖陪伴  │  ← 一行说明
│                    │
│  按模式键切换      │
│  按开始键确认      │
└────────────────────┘
```

### 3.3 监测主画面 (`DEVICE_MONITORING`) —— 最复杂

```
┌────────────────────┐
│ [严格/鼓励] 01:23:45│  ← 模式标签 + 总计时
│                    │
│    ┌──────────┐    │
│    │          │    │
│    │  状态    │    │  ← 80×80 状态图标
│    │  图标    │    │
│    │          │    │
│    └──────────┘    │
│                    │
│ 有效 01:02:30      │  ← 有效时长
│ 分心      3 次     │  ← 分心计数
│                    │
│ 专注度 ██████░░ 72 │  ← 评分 + 进度条
│                    │
│ ─── 鼓励模式专属 ───│
│  已专注 30 分钟!   │  ← 里程碑提示 (仅 GENTLE)
│  ████████░░ 30/60 │  ← 距下次里程碑进度条
└────────────────────┘
```

### 3.4 消息覆盖层（提醒/鼓励时）

```
┌────────────────────┐
│                    │
│      ⚠ / ★         │  ← 警告图标 (红) 或 鼓励图标 (绿)
│                    │
│  "请放下手机!"     │  ← 来自 study_state_t.message
│                    │  ← 3 秒后自动消失
│                    │
└────────────────────┘
```

**关键约束**：
- `REMIND` 类型：红色背景闪烁 (500ms 周期)，持续 3 秒
- `ENCOURAGE` 类型：绿色背景渐变，持续 3 秒
- 3 秒后自动返回监测主画面，**不需要按键确认**
- `lcd_show_message()` 必须在 100ms 内返回（异步显示由 ui_task 管理）

### 3.5 报告画面 (`DEVICE_REPORT`)

```
┌────────────────────┐
│    学习报告         │
│                    │
│ 模式: 严格模式     │
│ 总时长   2h 30min  │
│ 有效学习 2h 05min  │
│                    │
│ 分心明细:          │
│   玩手机   2 次    │
│   看手机   1 次    │
│   离座     0 次    │
│   瞌睡     1 次    │
│                    │
│ 专注度 83 分 良好  │
│                    │
│ ── MiMo 建议 ──    │
│ 你今天下午注意力   │
│ 比较集中...        │
│                    │
│ 按开始键返回       │
└────────────────────┘
```

---

## 四、状态图标设计

240×240 分辨率下，每个图标为 80×80 RGB565。

### 严格模式

| 状态 | 图标 | 颜色 |
|---|---|---|
| 专注 | 圆形 + ✓ | 绿 `#00C853` |
| 看手机 | 三角形 + 手机剪影 | 黄 `#FFD600` |
| 玩手机 | 圆形闪烁 + 手机+禁止符 | 红 `#D50000` |
| 离座 | 菱形 + 空椅子 | 灰 `#9E9E9E` |
| 瞌睡 | 圆形 + ZZZ | 蓝 `#448AFF` |

### 鼓励模式

| 状态 | 图标 | 颜色 |
|---|---|---|
| 专注 | 圆形 + ★ | 绿 `#00C853` |
| 看手机 | 圆形 + 手机+问号 | 橙 `#FF9800` |
| 玩手机 | 圆形 + 手机+小铃铛 | 橙 `#FF9800` |
| 离座 | 圆形 + 门 | 蓝灰 `#607D8B` |
| 瞌睡 | 圆形 + 咖啡杯 | 蓝 `#448AFF` |

> **原则**：鼓励模式不使用红色警告色，全部用暖色调（橙/蓝灰）替代，降低焦虑感。

### 图标存储方式

```c
// 5 种状态 × 2 种模式 = 10 个图标，每个 80×80×2 = 12.8 KB
// 总共 128 KB，存 Flash，启动时按需加载当前模式对应的 5 个
const uint16_t icons[2][5][80*80];  // [mode][status]
```

---

## 五、MiMo 大模型接入

### 调用时机

学习结束时（`DEVICE_REPORT` 画面加载前），由万思源的状态机调用：

```c
// 万思源会调这个函数（你在 lcd.c 中实现）
int mimo_get_advice(session_stats_t *stats,
                    uint8_t distraction_by_type[4],
                    char *advice_out, size_t max_len);
```

### 实现逻辑

```c
int mimo_get_advice(session_stats_t *stats,
                    uint8_t *by_type, char *out, size_t max) {
    // 1. 构造 JSON 请求
    char req[512];
    snprintf(req, sizeof(req),
        "{"
        "  \"total_min\": %u,"
        "  \"effective_min\": %u,"
        "  \"distractions\": [%u,%u,%u,%u],"
        "  \"focus_score\": %u,"
        "  \"mode\": \"%s\""
        "}",
        stats->total_duration_sec / 60,
        stats->effective_duration_sec / 60,
        by_type[0], by_type[1], by_type[2], by_type[3],
        stats->focus_score,
        stats->current_mode == 0 ? "strict" : "gentle"
    );

    // 2. HTTP POST 到 MiMo API
    int ret = http_post(MIMO_API_URL, req, out, max);

    // 3. 超时/失败降级 → 本地模板
    if (ret < 0) {
        if (stats->current_mode == 0) {
            snprintf(out, max, "严格模式下你坚持了 %u 分钟,下次减少分心次数会更好!",
                     stats->effective_duration_sec / 60);
        } else {
            snprintf(out, max, "你今天有效学习了 %u 分钟,已经很棒了!继续加油!",
                     stats->effective_duration_sec / 60);
        }
    }

    return 0;  // 永远返回 0（调用方不处理错误，降级后的 out 仍然可显示）
}
```

### 约束

- **超时**：单次 HTTP 请求超时 5 秒
- **降级**：失败时用本地模板生成建议，**不能返回空字符串**
- **不阻塞**：报告画面可以先显示，MiMo 建议异步获取后更新

---

## 六、开发约束

### 6.1 你必须遵守的

| 约束 | 说明 |
|---|---|
| **不要轮询状态** | UI 变化由状态机通过 `lcd_show_*` 推给你，不需要你自己去读全局变量 |
| **不要直接访问硬件** | 用张沐泽的 `camera_*` 不行。你的代码只操作 framebuffer（张沐泽在 `lcd_init` 里帮你初始化好） |
| **不要阻塞** | `lcd_show_*` 函数必须在 50ms 内返回。SPI DMA 发送是异步的，你的函数只是把数据写入 framebuffer |
| **不要分配超过 128KB 的静态内存** | 总共 10 个图标 128KB + framebuffer 115KB ≈ 243KB，8MB PSRAM 够用但别浪费 |
| **消息文本来源** | 直接显示 `study_state_t.message`，不要自己写提醒文案。文案由赵思涵统一管理 |
| **模式感知** | 所有 UI 元素必须根据 `session_stats_t.current_mode` 切换样式（图标、颜色、进度条、话术格式） |

### 6.2 你可以假设的

| 假设 | 说明 |
|---|---|
| `lcd_init()` 之后 framebuffer 已经映射好 | 张沐泽保证 `/dev/fb0` 可读写 |
| 调用 `lcd_show_*` 时传入的指针有效 | 万思源保证不会传 NULL 或野指针 |
| `study_state_t.message` 不是空字符串 | 赵思涵保证至少有一个 `\0` |
| 只在 `DEVICE_REPORT` 时调用 MiMo | 万思源保证不会在其他状态调用 `mimo_get_advice` |

### 6.3 编译集成

```makefile
# Makefile 中你的模块
ui_LIB  = libui.a
ui_SRCS = lcd.c lcd_icons.c mimo.c
ui_INC  = -I../api

# 张沐泽负责在你之前编译 lcd 底层驱动 (SPI + ST7789V 初始化)
# 你只需要在 lcd_init() 里调他提供的 st7789v_init()
```

---

## 七、开发建议

### 7.1 可以先做的最小可用版本 (MVP)

1. 只做监测主画面（其他画面用纯文字替代）
2. 严格模式的 5 个图标先做单色版本
3. 鼓励模式的图标先复用严格的（后期再替换）
4. MiMo 先用本地模板，API 最后一轮再接入

### 7.2 可以独立测试的方式

```c
#ifdef UI_UNIT_TEST
int main() {
    lcd_init();

    // 模拟万思源给你发状态
    session_stats_t mock = {
        .total_duration_sec = 3661,
        .effective_duration_sec = 3000,
        .distraction_count = 2,
        .focus_score = 82,
        .current_mode = 0,  // STRICT
    };

    study_state_t ss = {
        .status = FOCUSED,
        .action = NONE,
    };

    lcd_show_status(DEVICE_MONITORING, &mock, &ss);
    sleep(3);

    // 测试报告
    session_report_t report = { .stats = mock };
    lcd_show_report(&report, "测试建议文本");
    sleep(3);

    lcd_show_message("请放下手机!", REMIND);

    return 0;
}
#endif
```

### 7.3 和其他人的联调顺序

```
  第 1 步: 跟张沐泽联调 → 确认 LCD 能显示色块和文字
  第 2 步: 跟万思源联调 → 状态变化时 UI 正确切换
  第 3 步: 跟赵思涵联调 → 提醒消息正确显示
  第 4 步: MiMo 独立测试  → 最后接入
```
