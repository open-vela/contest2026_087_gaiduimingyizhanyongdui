# FOCUS AIoT - 状态机采集时序修正（开发需求）

> 状态：待开发（准备新开对话处理）| 提出：万思源 | 日期：2026-08-27
> 目标：修正"摄像头采集"与"状态机模式选择"的时序关系。

---

## 一、问题描述（当前行为）

当前主循环 `hello_app_main.c` 里，摄像头采集**无条件、每 5 秒一次**地执行，
与状态机状态无关：

```
for (;;) {
    state_machine_tick();          // 10Hz 状态机
    if (++frame_tick >= 50) {      // 每 5s
        camera_capture_frame(...)  // ← 无条件采集
        rgb565_to_jpeg(...)
        perception_process(...)    // ← 云端识图，阻塞数秒
    }
    usleep(100ms);
}
```

由此产生的问题：

1. **一进 `hello_app`（IDLE）就开始采集**：长按进模式选择之前，
   `[cam] #1` 就已经触发一次采集+识图（阻塞数秒）。
2. **模式选择界面刷新滞后**：长按后要等**第一帧识图完成**（数秒阻塞）才
   刷新为模式选择界面；短按选模式后，又要等**第二帧识图完成**才刷新为
   对应模式的监测界面。界面响应被"识图延迟"拖累。
3. **超长按停止后仍在采集**：停止（进入 REPORT）后，采集循环没有停，
   还在每 5s 采一帧 + 调云端（浪费流量/电量，且无意义）。

## 二、期望行为

| 状态 | 是否采集 | 说明 |
|---|---|---|
| DEVICE_IDLE（待机） | ❌ 不采集 | 进 hello_app 就停在待机页 |
| DEVICE_MODE_SELECT（模式选择） | ❌ 不采集 | 长按后**立即**刷新为模式选择界面（不等识图） |
| DEVICE_MONITORING（监测中） | ✅ 采集 | 短按确认模式后**开始**采集+识图 |
| DEVICE_REPORT（报告页） | ❌ 不采集 | 超长按停止后**立即停止**采集 |

即：**确定模式之后才开始采集图像，超长按停止之后结束采集图像。**

## 三、技术方案

### 3.1 采集门控（hello_app_main.c 主循环）

用 `state_machine_get_status()`（`api/state_machine.h`）判断当前状态，
只在 `DEVICE_MONITORING` 时执行采集分支：

```c
// 伪代码
device_status_t st = state_machine_get_status();
if (++frame_tick >= 50) {
    frame_tick = 0;
    if (st == DEVICE_MONITORING) {
        // 采集 → JPEG → 识图 → obs
    }
}
```

- 状态机 `tick` 继续每 10Hz 跑（驱动 UI/行为），但**采集只发生在 MONITORING**。
- 需要确认 `state_machine_get_status()` 在 IDLE/MODE_SELECT/MONITORING/REPORT
  各状态下的返回值准确（检查 `core/state_machine.c`）。

### 3.2 界面刷新不等识图

- 长按进 MODE_SELECT：应在**按键事件处理完立刻** `lcd_show_status(...)`，
  不等待采集/识图线程。检查按键事件 → 状态转换 → UI 刷新是否串行阻塞。
- 短按确认模式 → MONITORING：立即刷新对应模式的监测界面，
  第一帧识图结果到了再更新界面上"状态"字段。

### 3.3 相关文件

| 文件 | 改动 |
|---|---|
| `app/hello_app/hello_app_main.c` | 主循环采集门控；UI 刷新时机 |
| `app/hello_app/core/state_machine.c` | 确认各状态返回；必要时补"停止即停采"逻辑 |
| `app/hello_app/api/state_machine.h` | 接口（应已冻结，尽量不改） |

## 四、验收标准

1. 进 `hello_app`（IDLE）串口**不出现 `[cam]` 采集日志**。
2. 长按 → **立即**刷新模式选择界面（串口 `IDLE -> MODE_SELECT` 之后，
   界面不等识图），且 MODE_SELECT 期间不采集。
3. 短按确认模式 → MONITORING，**才开始** `[cam] #1 尝试采集...`。
4. 超长按停止 → REPORT，串口**不再出现新的 `[cam]`**。

## 五、联调注意事项

- 摄像头连续采集依赖 PR #14 的 REQBUFS 重置修复（`camera_capture_frame`
  每轮先 REQBUFS 重建容器链），不要改回 BUFCOUNT=1。
- 识图每帧会阻塞数秒（MiMo 推理），MONITORING 期间 UI 刷新要容忍该延迟，
  或考虑把采集/识图挪到独立任务（后续优化项，本期可不做）。

---

## 附：开发环境 / 代理 / PR 方式（给新对话）

### 构建环境

- 工作区：`/home/ubuntu/openvela`（openvela + NuttX，make 构建，非 cmake）
- 板卡：esp32s3-eye；构建命令：
  ```
  cd /home/ubuntu/openvela
  export PATH=/home/ubuntu/vela-opensource/prebuilts/gcc/linux-x86_64/xtensa-esp32s3-elf/bin:$PATH
  make -C nuttx -j2        # 增量构建；全量用 ./build.sh <defconfig> -j2
  ```
- 产物：`nuttx/nuttx.bin`，烧录：
  ```
  esptool -c esp32s3 -p COM6 -b 460800 --before default-reset --after hard-reset write_flash 0x0 nuttx.bin
  ```
- **改 STACKSIZE 后必须重建 builtin 注册表**：
  ```
  rm -f apps/.context apps/builtin/registry/.updated apps/builtin/builtin_list.h apps/builtin/builtin_proto.h
  make -C nuttx context
  ```

### 代理（GitHub/下载）

```
export http_proxy=http://192.168.253.1:7892 https_proxy=http://192.168.253.1:7892
```

### PR 方式

- 比赛仓：`/home/ubuntu/openvela/contest2026_087_gaiduimingyizhanyongdui`
- remote：`fork` = `Skadi-QL/contest2026_087_gaiduimingyizhanyongdui`，
  `openvela` = `open-vela/contest2026_087_gaiduimingyizhanyongdui`
- 上游目标分支：**`dev-ai-contest-2026`**
- 流程：新分支（从 `openvela/dev-ai-contest-2026` 切出）→ 提交 → push 到 fork →
  用 GitHub API 建 PR（token 用 `<GitHub_TOKEN>` 占位，实际值见 Claude 会话环境，
  **切勿把真实 token 写进提交/文档**——GitHub 密钥扫描会拦截推送）：
  ```
  curl -s -X POST https://api.github.com/repos/open-vela/contest2026_087_gaiduimingyizhanyongdui/pulls \
    -H "Authorization: token <token>" -H "Accept: application/vnd.github.v3+json" \
    -d '{"title":"...","head":"Skadi-QL:<分支名>","base":"dev-ai-contest-2026","body":"..."}'
  ```
- 合并由万思源在 GitHub 上处理。

### 已合并的 PR

- PR #13（feat/integration-perception）：栈 32K / 摄像头 REQBUFS / wifi HTTP 大 body / 打点
- PR #14（feat/mimo-relay-chain）：MiMo 识图链路（prompt/解析/缓冲/token/超时）+ 中继脚本 + 文档
